EasyCommands

EasyCommands is a lightweight all-in-one utility plugin for Rust servers, focused on clean, customizable player commands and automation.

It’s built to be simple to configure, easy to extend, and performance-friendly.

🔧 Features

✅ In-game Time Command

Players can type a configurable command (default: !time) to see the in-game Rust time.
Supports time-only or date time
Fully customizable message output
Easily toggleable in config

✅ Pop Command

Players can type a configurable command (default: !pop) to see how many players are currently online.
Fully customizable message output
Easily toggleable in config

✅ Wipe Schedual Command

Players can type a configurable command (default: !wipe) to see the next wipe date/time.
Another command (default: !bpwipe) can be used to see the next bp wipe date/time.
Fully customizable message output
Easily toggleable in config
No manual setup each wipe, auto calcuates wipe dates

✅ Automatic Skip-Night Voting

Automatically starts a vote at a configurable in-game time every night (default: 18:00).
No player abuse – votes are scheduled, players only vote
Configurable command (default: !skip)
Configurable percentage of players required
Customizable messages
Resets automatically each day

✅ Admin Whisper Command

Allows admins or the server console to privately message players.
Perfect for welcome messages, warnings, or support
Designed to work with RustAdmin / RCON automation
Fully customizable message formats

✅ Global Styling

Optional global prefix
Custom prefix & message colors
All messages configurable via JSON

